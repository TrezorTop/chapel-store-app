datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model User {
    username String @id
    passwordHash String
    tokens RefreshToken[]
}


model RefreshToken {
    token String @id
    owner User @relation(fields: [ownerId], references: [id])
    ownerId Int
}